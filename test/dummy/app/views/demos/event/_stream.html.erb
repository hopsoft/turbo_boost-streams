<%= turbo_stream
  .invoke(:dispatch_event, args: ["turbo-ready:demo"])
  .invoke("document.dispatchEvent", args: ["turbo-ready:demo"])
  .invoke(
    :dispatch_event,
    args: [
      "turbo-ready:demo",
      {bubbles: true, detail: {source: "The invoke Turbo Stream from TurboReady"}}
    ],
    selector: "button"
  ) %>
